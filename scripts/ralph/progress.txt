# Ralph Progress Log
Started: Fri Apr  3 20:44:47 EDT 2026
---

## Codebase Patterns
- macOS project builds with: `xcodebuild -project QuipMac/QuipMac.xcodeproj -scheme QuipMac -configuration Debug build`
- QuipLinux cannot be typechecked on macOS — requires GTK/GLib system libs only available on Linux. Verify Rust changes are type-safe by inspection.
- AppleScript string escaping uses `escapeForAppleScript()` in KeystrokeInjector.swift — only escapes `\` and `"` (sufficient for `keystroke`, NOT for `do script`)
- Terminal.app text injection uses System Events keystrokes; iTerm2 uses `write text` (iTerm2's API is safe as it doesn't interpret shell)
- Multi-line text for Terminal.app must be split on newlines, each line typed with `keystroke`, then `key code 36` (Return) between lines
- Rust protocol messages: use `Deserialize` only for incoming (client→server), `Serialize` only for outgoing (server→client). Use `#[serde(skip_serializing_if = "Option::is_none")]` for optional fields.
- Protocol changes touch 5 files: Shared/MessageProtocol.swift, Protocol.kt, messages.rs, protocol.md, MessageProtocolTests.swift
- Android Gradle build is broken (JDK version mismatch) — pre-existing, not caused by protocol changes
- New services: add file to Xcode project.pbxproj (PBXBuildFile, PBXFileReference, PBXGroup children, PBXSourcesBuildPhase) and inject via `.environment()` in QuipMacApp.swift
- Keychain storage pattern: use `kSecClassGenericPassword` with `kSecAttrAccount` as key, `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` for sensitive data
- PINManager is `@Observable` — auto-generates on first launch, stores in Keychain, accessed via `@Environment(PINManager.self)`
- WebSocketServer auth: connections start unauthenticated, only "auth" messages processed until authenticated. Set `pinManager` property before calling `start()`.
- Per-connection send uses private `send(_:to:)` method; broadcast only goes to authenticated clients.
- Linux WsServer uses HashMap<usize, ClientConnection> with per-connection auth state (not Vec<WsSink>)
- PINManager is shared via Clone (Arc<RwLock<String>> interior) — same instance for UI and WsServer
- Settings dialog `show_settings` requires `&PINManager` parameter
- Android network_security_config.xml cannot express IP CIDR ranges — use code-level validation (NetworkValidator.kt) for private IP restrictions
- PIN stored at XDG_CONFIG_HOME/quip/pin (defaults to ~/.config/quip/pin), file 0600, dir 0700
- iOS auth flow: connection established → onAuthRequired callback → PIN entry UI → sendAuth(pin:) → auth_result handler. Cached PIN auto-sent on reconnect.
- iOS UI state gates: use `client.isAuthenticated` (not `isConnected`) for enabling controls, landscape mode, and keyboard toggle
- Android auth flow: connect → onAuthRequired callback → PIN entry UI (MainScreen) → sendAuth() → auth_result. Cached PIN auto-sent on reconnect. Controls gated on `isAuthenticated`.
- Android UI state: `showPinEntry` controls PIN screen visibility; `isAuthenticated` gates LayoutScreen and landscape orientation
- macOS tunnel log path: `~/Library/Caches/Quip/tunnel.log` (via FileManager cachesDirectory). Dir 0700, file 0600.
- SecretRedactor (macOS) is a static enum with regex patterns; Linux equivalent is `secret_redactor::redact()` using `LazyLock<Vec<RedactPattern>>`
- Rate limiting: per-client counter + window start timestamp in ClientConnection; `allowMessage()` resets counter when ≥1s elapsed, drops when count > 10
- Linux tunnel log path: `XDG_CACHE_HOME/quip/tunnel.log` (defaults to `~/.cache/quip/tunnel.log`). Dir 0700, file 0600. Uses `prepare_tunnel_log()` helper.
- Message size limit: 64KB (65,536 bytes) — check before any processing, drop silently, keep connection alive
- Certificate pinning: pin intermediate + root CA SPKI hashes (not leaf); trycloudflare.com uses Google Trust Services chain (WE1 → GTS Root R4)
- iOS typecheck without provisioning: `swiftc -typecheck -sdk $(xcrun --sdk iphoneos --show-sdk-path) -target arm64-apple-ios17.0 <files>`
- OkHttp CertificatePinner only applies to HTTPS/WSS — no special handling needed to skip ws:// connections
- URL trust validation: trusted = wss://*.trycloudflare.com OR ws:// to private IPs (10/8, 172.16/12, 192.168/16, loopback, link-local, localhost)
- Audit logging: AuditLogger (macOS) is a static enum with background DispatchQueue; log to ~/Library/Logs/Quip/audit.log, 10MB rotation
- Linux XDG paths: config=XDG_CONFIG_HOME (~/.config), cache=XDG_CACHE_HOME (~/.cache), data=XDG_DATA_HOME (~/.local/share) — each under /quip/ subdir
---

## 2026-04-03 - US-001
- Replaced `do script` with System Events `keystroke` for all Terminal.app text injection
- Split multi-line text into individual lines, type each with `keystroke`, press Return (key code 36) between lines
- Each line individually escaped via `escapeForAppleScript()`
- iTerm2 behavior unchanged (uses `write text` which is safe)
- Files changed: QuipMac/Services/KeystrokeInjector.swift
- **Learnings for future iterations:**
  - `do script` in Terminal.app AppleScript executes text as a shell command — never use it with untrusted input
  - System Events `keystroke` types characters literally, so shell metacharacters ($, `, |, etc.) are safe
  - The `escapeForAppleScript` function only escapes `\` and `"` — sufficient for AppleScript string literals but not for shell interpretation
  - Input `text` can contain real newline characters; must split on `"\n"` not `"\\n"`
---

## 2026-04-03 - US-002
- Changed `0.0.0.0` to `127.0.0.1` in ws_server.rs bind address (line 32)
- Cloudflare tunnel connects locally so it is unaffected
- Files changed: QuipLinux/src/services/ws_server.rs
- **Learnings for future iterations:**
  - QuipLinux depends on GTK/GLib and cannot `cargo check` on macOS — only on a Linux host
  - The bind address is a string literal format arg, so changing it has zero type implications
---

## 2026-04-03 - US-003
- Added `AuthMessage` (type: "auth", pin: String) and `AuthResultMessage` (type: "auth_result", success: Bool, error: String?) to all platform protocol files
- Updated protocol documentation in docs/protocol.md with Authentication section
- Added encoding, decoding, and round-trip tests for both auth message types
- Files changed: Shared/MessageProtocol.swift, QuipAndroid/.../Protocol.kt, QuipLinux/src/protocol/messages.rs, docs/protocol.md, QuipiOS/Tests/MessageProtocolTests.swift
- **Learnings for future iterations:**
  - Shared/MessageProtocol.swift is used by BOTH QuipMac and QuipiOS projects — changes compile-check via either xcodebuild
  - Android Gradle build is broken due to JDK version mismatch (pre-existing) — Kotlin changes verified by pattern inspection only
  - iOS build requires provisioning profile and connected device or simulator — no simulators installed on this machine
  - Rust `AuthMessage` only needs `Deserialize` (server receives it), `AuthResultMessage` only needs `Serialize` (server sends it)
  - Use `#[serde(skip_serializing_if = "Option::is_none")]` on optional fields in Rust to match Swift/Kotlin null-omission behavior
---

## 2026-04-03 - US-004
- Created PINManager.swift service: auto-generates 6-digit PIN on first launch, stores/loads from Keychain
- Added Security tab to SettingsView with PIN display (monospaced, selectable), copy button, and regenerate button
- Injected PINManager into app via `.environment()` in QuipMacApp.swift Settings scene
- Updated Xcode project.pbxproj with new file references
- Files changed: QuipMac/Services/PINManager.swift (new), QuipMac/QuipMacApp.swift, QuipMac/Views/SettingsView.swift, QuipMac/QuipMac.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - Keychain requires delete-before-add pattern (SecItemDelete then SecItemAdd) — there's no upsert
  - `@Observable` classes with `init()` that loads state work well for auto-generate-on-first-launch patterns
  - SettingsView uses private structs for each tab — follow the same pattern for new tabs
  - PINManager only needs to be injected into Settings scene (not MainWindow or MenuBarExtra) for US-004, but US-005 will need it in the main app for auth enforcement
---

## 2026-04-03 - US-005
- Enforced PIN authentication on macOS WebSocket server
- New connections start in "pending auth" state — all non-auth messages silently dropped until authenticated
- Auth messages handled directly in WebSocketServer (not forwarded to app handler)
- PINManager wired into WebSocketServer via property injection in startServicesOnce()
- Broadcast now only sends to authenticated clients
- Added per-connection send method for auth result messages
- Files changed: QuipMac/Services/WebSocketServer.swift, QuipMac/QuipMacApp.swift
- **Learnings for future iterations:**
  - Auth gating lives in the WebSocket server layer, not the app message handler — keeps separation clean
  - `NWConnection` is a class (reference type), so `===` identity comparison works for tracking connections
  - `ClientConnection` struct wraps NWConnection with auth state — mutated via index lookup in the clients array
  - `broadcast()` filters to authenticated clients only, so layout updates don't leak to unauthenticated connections
---

## 2026-04-03 - US-006
- Created PINManager service: generates 6-digit PIN, stores in ~/.config/quip/pin (XDG_CONFIG_HOME aware)
- Refactored WsServer to use HashMap<usize, ClientConnection> with per-connection auth state
- New connections start unauthenticated; only "auth" messages processed until PIN verified
- Auth result sent back to specific client via per-connection sink
- Broadcast only targets authenticated clients
- Added Security page to settings dialog with PIN display, copy, and regenerate buttons
- PINManager shared between settings UI and WsServer via Arc<RwLock<String>> interior
- Added `rand` 0.9 dependency for PIN generation
- Files changed: QuipLinux/src/services/pin_manager.rs (new), QuipLinux/src/services/ws_server.rs, QuipLinux/src/services/mod.rs, QuipLinux/src/ui/main_window.rs, QuipLinux/src/ui/settings_dialog.rs, QuipLinux/Cargo.toml, QuipLinux/Cargo.lock
- **Learnings for future iterations:**
  - PINManager uses Arc<RwLock<String>> so clones share state — regeneration in settings UI immediately affects WsServer auth checks
  - WsServer now uses HashMap<usize, ClientConnection> instead of Vec<WsSink> — each client has a unique ID for targeted sends and cleanup
  - `rand::random_range(0..N)` is the rand 0.9 free function for generating random numbers in a range
  - Settings dialog `show_settings` now takes a `&PINManager` parameter — any callers must be updated
  - The broadcast call in `handle_incoming_message` (RequestContent handler) was already calling async broadcast without `.await` — pre-existing issue, not introduced here
---

## 2026-04-03 - US-007
- Added PIN authentication flow to iOS client (WebSocketClient + MainiOSView)
- WebSocketClient: added `isAuthenticated`, `authError`, `sessionPIN` state; handles `auth_result` messages; gates data messages behind auth; auto-sends cached PIN on reconnect
- MainiOSView: new `authenticatingBar` with PIN entry UI (numeric keypad, 6-digit input, submit button, error display); controls gated on `isAuthenticated` not just `isConnected`; orientation locked to portrait until authenticated
- Files changed: QuipiOS/Services/WebSocketClient.swift, QuipiOS/QuipApp.swift
- **Learnings for future iterations:**
  - iOS build requires provisioning profile — typecheck via macOS build (shared MessageProtocol.swift compiles in both)
  - `@Bindable var client: WebSocketClient` in SwiftUI views auto-tracks `@Observable` property changes — no manual `objectWillChange` needed
  - Connection state flow is now: disconnected → connecting → connected (unauthenticated) → authenticated; landscape mode tied to authenticated, not connected
  - PIN cached in `sessionPIN` for seamless reconnects — cleared on intentional disconnect or failed auth (bad PIN)
  - `onAuthRequired` callback fires when connection established but no cached PIN; `onAuthResult` fires on server response
---

## 2026-04-03 - US-008
- Added PIN authentication flow to Android client (WebSocketClient + MainViewModel + MainScreen)
- WebSocketClient: added `isAuthenticated`, `authError`, `sessionPIN` state; handles `auth_result` messages; gates data messages behind auth; auto-sends cached PIN on reconnect
- MainViewModel: added `isAuthenticated`, `authError`, `showPinEntry`, `pinText` state; wires auth callbacks; `sendAuth()` method; gates landscape orientation behind auth
- MainScreen: added `PinEntryContent` composable with numeric keypad, 6-digit input (min 4 to submit), error display, disconnect button; screen shown when connected but not authenticated
- Files changed: QuipAndroid/.../services/WebSocketClient.kt, QuipAndroid/.../MainViewModel.kt, QuipAndroid/.../ui/screens/MainScreen.kt, QuipAndroid/.../MainActivity.kt
- **Learnings for future iterations:**
  - Android auth mirrors iOS pattern: connect → onAuthRequired → PIN entry UI → sendAuth → auth_result handler
  - `@Volatile` on WebSocketClient state fields ensures thread-safe reads from main thread when writes happen on OkHttp threads
  - Android Gradle build still broken (JDK mismatch) — changes verified by inspection per established pattern
  - MainScreen uses named parameters with defaults for backward-compatible additions — new auth params added between existing groups
  - PIN "pinning" in ConnectionManager is unrelated to auth PIN — naming collision but no code conflict
---

## 2026-04-03 - US-009
- Moved tunnel log from /tmp/quip_tunnel.log to ~/Library/Caches/Quip/tunnel.log
- Log directory created with 0700 permissions, log file with 0600 permissions
- Used FileManager.cachesDirectory for proper macOS path resolution
- Replaced `try? "".write(...)` with `createFile(atPath:contents:attributes:)` for permission control
- Files changed: QuipMac/Services/CloudflareTunnel.swift
- **Learnings for future iterations:**
  - `FileManager.default.urls(for: .cachesDirectory, in: .userDomainMask)` resolves to ~/Library/Caches on macOS
  - Use `createFile(atPath:contents:attributes:)` instead of `String.write(toFile:)` when you need to set file permissions
  - `logPath` changed from `static let` to `static var` (computed) since it depends on FileManager at runtime
---

## 2026-04-03 - US-010
- Moved Linux tunnel log from no-log-at-all to ~/.cache/quip/tunnel.log (XDG_CACHE_HOME aware)
- Added `tunnel_log_path()` and `prepare_tunnel_log()` helper functions
- Directory created with 0700 permissions, log file with 0600 permissions
- Stderr reader thread now writes each line to the log file while also parsing for the tunnel URL
- Moved `PermissionsExt` import to top level (was duplicated in `download_cloudflared`)
- Files changed: QuipLinux/src/services/cloudflare_tunnel.rs
- **Learnings for future iterations:**
  - Linux cloudflare_tunnel.rs previously had no log file — it only parsed stderr in-memory, unlike macOS which wrote to a file
  - `XDG_CACHE_HOME` defaults to `~/.cache` per XDG Base Directory spec — same pattern as `XDG_CONFIG_HOME` used by PINManager
  - `std::fs::OpenOptions::new().append(true).open()` is idiomatic for appending to existing files in Rust
  - `prepare_tunnel_log()` creates/truncates the file fresh each tunnel start, then the stderr thread opens it in append mode
- Cloudflare publishes `.sha256` files at `{binary_url}.sha256` — format is `<hash>  <filename>`
---

## 2026-04-03 - US-011
- Added SHA256 verification for cloudflared auto-download in `download_cloudflared()`
- After downloading binary, downloads `.sha256` checksum file from same GitHub release URL
- Computes local SHA256 via `sha256sum` command, compares against published hash
- On mismatch: deletes the unverified binary, logs error, returns failure (binary never executed)
- On success: proceeds with chmod and execution as before
- Files changed: QuipLinux/src/services/cloudflare_tunnel.rs
- **Learnings for future iterations:**
  - Cloudflare publishes `.sha256` files alongside binaries at `{binary_url}.sha256`
  - The `.sha256` file format is `<hash>  <filename>` — parse first whitespace-delimited token for the hash
  - Using `sha256sum` command avoids adding `sha2`+`hex` crate dependencies; acceptable since `curl` is already a runtime dependency
  - Verification must happen BEFORE `chmod +x` — never make an unverified binary executable
  - `tokio::process::Command` already used in the download function, so the verify function uses the same async pattern
---

## 2026-04-03 - US-012
- Restricted Android cleartext traffic to local/private networks using a two-layer approach
- Updated network_security_config.xml: added domain-config enforcing TLS for trycloudflare.com (defense-in-depth), documented why base-config must stay cleartextTrafficPermitted="true"
- Created NetworkValidator.kt: validates ws:// URLs target only private IPs (RFC 1918 via InetAddress.isSiteLocalAddress, loopback, link-local, localhost)
- Added URL validation in MainViewModel.connect() — rejects ws:// to non-private networks before connecting
- Files changed: QuipAndroid/app/src/main/res/xml/network_security_config.xml, QuipAndroid/app/src/main/java/dev/quip/android/services/NetworkValidator.kt (new), QuipAndroid/app/src/main/java/dev/quip/android/MainViewModel.kt
- **Learnings for future iterations:**
  - Android's network_security_config.xml `<domain>` cannot express IP CIDR ranges — `includeSubdomains` only works for DNS subdomain hierarchy, not IP subnets
  - Setting `base-config cleartextTrafficPermitted="false"` blocks OkHttp cleartext connections to ALL non-whitelisted hosts (checked via `NetworkSecurityPolicy.isCleartextTrafficPermitted(hostname)`) — there is no way to whitelist IP ranges in XML
  - For apps that connect to local IPs via ws://, the security enforcement must be split: XML for domain-based restrictions, code for IP-range restrictions
  - `InetAddress.isSiteLocalAddress` covers exactly RFC 1918 ranges (10/8, 172.16/12, 192.168/16); `isLoopbackAddress` covers 127/8; `isLinkLocalAddress` covers 169.254/16
  - OkHttp 4.12.0 normalizes ws:// to http:// internally, so the platform cleartext check applies to WebSocket connections
---

## 2026-04-03 - US-013
- Added SecretRedactor.swift service with regex-based pattern matching for common secret types
- Patterns redacted: sk-* (OpenAI), ghp_/gho_/ghs_/ghr_* (GitHub), AKIA* (AWS key IDs), AWS secret keys, Bearer tokens, token: values
- Applied redaction in request_content handler (QuipMacApp.swift) before broadcasting terminal content
- Added SecretRedactor.swift to Xcode project.pbxproj
- Files changed: QuipMac/Services/SecretRedactor.swift (new), QuipMac/QuipMacApp.swift, QuipMac/QuipMac.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - SecretRedactor uses NSRegularExpression with capture groups to preserve labels (e.g., "Bearer " prefix) while redacting the secret value
  - Redaction applied to trimmed text (after 200-line limit) in the request_content handler, before broadcast
  - Patterns use minimum length thresholds (e.g., 20+ chars) to avoid false positives on short strings
  - US-014 (Linux equivalent) should use the same patterns but in Rust regex syntax
---

## 2026-04-03 - US-014
- Created secret_redactor.rs with same regex patterns as macOS SecretRedactor.swift, translated to Rust regex syntax
- Applied redaction in request_content handler (main_window.rs) after trimming, before broadcast — same position as macOS
- Registered module in services/mod.rs
- Files changed: QuipLinux/src/services/secret_redactor.rs (new), QuipLinux/src/services/mod.rs, QuipLinux/src/ui/main_window.rs
- **Learnings for future iterations:**
  - Rust `regex` crate uses `${1}` for capture group references in replacement strings (not `$1`)
  - `std::sync::LazyLock` is stable in Rust — no need for `once_cell` or `lazy_static` crates
  - The `regex` crate was already in Cargo.toml, so no dependency additions needed
  - Redaction sits between content trimming and TerminalContentMessage::new — shadow the `content` variable with `let content = redact(&content);`
---

## 2026-04-03 - US-015
- Added per-client rate limiting (10 messages/second) to macOS WebSocket server
- Extended `ClientConnection` struct with `messageCount`, `windowStart`, and `allowMessage()` method
- Rate check applied in `receiveMessage` before any message processing (including auth), using client index lookup
- Excess messages silently dropped; window resets after 1 second elapses
- Files changed: QuipMac/Services/WebSocketServer.swift
- **Learnings for future iterations:**
  - Rate limiting must use index-based mutation (`clients[index].allowMessage()`) since `ClientConnection` is a value type (struct)
  - Rate limit check goes before auth check in the receive handler — prevents unauthenticated flood attacks too
  - `Date().timeIntervalSince(windowStart) >= 1.0` is a simple 1-second sliding window approach; no timer needed
  - US-016 (Linux equivalent) should use the same pattern: per-client counter + timestamp in `ClientConnection` HashMap entry
---

## 2026-04-03 - US-016
- Added per-client rate limiting (10 messages/second) to Linux WebSocket server
- Extended `ClientConnection` struct with `message_count: u32` and `window_start: Instant` fields
- Added `allow_message()` method on `ClientConnection` impl block
- Rate check applied before auth check in message receive loop — prevents unauthenticated flood attacks
- Excess messages silently dropped with info log; window resets after 1 second
- Files changed: QuipLinux/src/services/ws_server.rs
- **Learnings for future iterations:**
  - `std::time::Instant` is the right choice for rate limiting in Rust (monotonic, no syscall overhead)
  - Rate limit check requires `get_mut` on the HashMap entry since `allow_message()` mutates state
  - The lock scope for rate limiting is separate from the auth check lock scope — drop the lock between checks to avoid holding it longer than needed
  - Pattern matches macOS exactly: per-client counter + timestamp, 10 msg/s cap, silent drop
---

## 2026-04-03 - US-017
- Added 64KB message size limit to macOS WebSocket server
- Size check applied in `receiveMessage` callback immediately after receiving data, before dispatching to main queue
- Oversized messages silently dropped with a log line; connection stays open, receive loop continues
- Files changed: QuipMac/Services/WebSocketServer.swift
- **Learnings for future iterations:**
  - Size check goes before the `DispatchQueue.main.async` block — no need to consume main thread for oversized messages
  - The `receiveMessage` closure captures `content` as `Data?` from NWConnection — `data.count` gives byte length directly
  - Must call `self.receiveMessage(on: connection)` after dropping to continue the receive loop (early return skips the one at the bottom)
  - US-018 (Linux equivalent) should apply the same pattern in the Rust WebSocket message receive loop
---

## 2026-04-03 - US-018
- Added 64KB message size limit to Linux WebSocket server
- Size check applied immediately after extracting text, before rate limit check and before logging the message content
- Oversized messages silently dropped with info log; connection stays open, receive loop continues via `continue`
- Files changed: QuipLinux/src/services/ws_server.rs
- **Learnings for future iterations:**
  - `const` inside a function body is fine in Rust — no need for a module-level constant for single-use values
  - Size check should go before rate limiting — don't count oversized messages against the rate limit
  - The `text.len()` in Rust gives byte length (UTF-8), which is the right metric for message size limiting
  - Pattern matches macOS exactly: 64KB cap, silent drop, connection stays alive
---

## 2026-04-03 - US-019
- Added certificate pinning for Cloudflare tunnel (wss://*.trycloudflare.com) on iOS
- Created `CloudflareCertificatePinningDelegate` (URLSessionDelegate) with SPKI SHA-256 hash validation
- Pins intermediate (WE1 / Google Trust Services) and root (GTS Root R4) CA public keys
- Local ws:// connections bypass pinning entirely — only wss:// uses the delegate
- Pin update mechanism documented in code comments and docs/protocol.md
- Files changed: QuipiOS/Services/WebSocketClient.swift, docs/protocol.md
- **Learnings for future iterations:**
  - trycloudflare.com currently uses Google Trust Services (WE1 intermediate, GTS Root R4 root), NOT Cloudflare's own CAs
  - `SecTrustCopyCertificateChain()` returns CFArray — must cast to `[SecCertificate]` before subscripting
  - `SecKeyCopyExternalRepresentation` extracts the raw public key data; hash with CC_SHA256 for SPKI pinning
  - URLSession delegate must be stored as a strong reference (property on the client) — otherwise it gets deallocated
  - Pin intermediate + root CAs, never the leaf (leaf rotates frequently)
  - iOS build requires provisioning profile — typecheck via `swiftc -typecheck` with iOS SDK directly
---

## 2026-04-03 - US-020
- Added certificate pinning for Cloudflare tunnel (wss://*.trycloudflare.com) on Android
- Used OkHttp's built-in `CertificatePinner` with same SPKI SHA-256 hashes as iOS (WE1 intermediate + GTS Root R4)
- Added `certificatePinner` to OkHttpClient builder in `establishConnection()`
- Local ws:// connections unaffected — CertificatePinner only applies to HTTPS/WSS
- Pin update docs already in docs/protocol.md (added during US-019)
- Files changed: QuipAndroid/.../services/WebSocketClient.kt
- **Learnings for future iterations:**
  - OkHttp `CertificatePinner` is much simpler than iOS — just add pins to the builder, no delegate needed
  - `CertificatePinner.add()` accepts vararg pins — can pass multiple hashes in one call
  - Pin format is `sha256/<base64-hash>` — same base64 SPKI hashes as iOS but with `sha256/` prefix
  - CertificatePinner only checks pins for HTTPS/WSS connections — cleartext ws:// is unaffected automatically
  - Android Gradle build still broken (JDK mismatch) — changes verified by inspection per established pattern
---

## 2026-04-03 - US-021
- Added `.claude/settings.local.json` to .gitignore
- File was not tracked by git, so no `git rm --cached` needed
- macOS build passes typecheck
- Files changed: .gitignore
- **Learnings for future iterations:**
  - Always check `git ls-files --error-unmatch` before running `git rm --cached` — avoids errors on untracked files
  - .gitignore sections are organized by category with comments — follow the pattern when adding new entries
---

## 2026-04-03 - US-022
- Added XML comment block in Info.plist directly above NSAppTransportSecurity dict explaining:
  - NSAllowsArbitraryLoads required for local ws:// WebSocket to dynamically-discovered desktop IPs
  - NSAllowsLocalNetworking is the narrower iOS 10+ exception
  - Cloudflare tunnel uses wss:// with certificate pinning (no ATS exception needed)
  - Cross-reference to docs/specs/security-audit.md L2 finding
- Files changed: QuipiOS/Info.plist
- **Learnings for future iterations:**
  - XML comments (`<!-- ... -->`) work fine in Apple plist files — Xcode preserves them
  - Info.plist documentation is best done inline with XML comments rather than a separate companion doc, since developers look at the plist first
  - macOS build still typechecks Info.plist changes since Shared/ is compiled in both projects
---

## 2026-04-03 - US-023
- Created AuditLogger.swift: static enum with background serial queue for non-blocking writes
- Logs send_text and quick_action messages to ~/Library/Logs/Quip/audit.log
- Log entries include: ISO8601 timestamp, client identifier, message type, text content (first 200 chars)
- Log directory created with 0700 permissions, log file with 0600 permissions
- Log rotation: when file exceeds 10MB, rotates to audit.log.1 (keeps one backup)
- Audit logging calls added in handleIncomingMessage() for send_text and quick_action cases
- Added AuditLogger.swift to Xcode project.pbxproj
- Files changed: QuipMac/Services/AuditLogger.swift (new), QuipMac/QuipMacApp.swift, QuipMac/QuipMac.xcodeproj/project.pbxproj
- **Learnings for future iterations:**
  - `FileHandle(forWritingTo:)` + `seekToEndOfFile()` + `write()` is the idiomatic append pattern in Swift (no need for OutputStream)
  - Background DispatchQueue with `.utility` QoS is ideal for audit logging — doesn't block message processing
  - Simple rotation (current → .1, delete old .1) is sufficient for a single 10MB cap — no need for numbered rotation
  - `chmod()` from C stdlib works directly in Swift for setting directory permissions after `createDirectory`
  - US-024 (Linux equivalent) should use the same pattern: background thread, ~/.local/share/quip/audit.log, 10MB rotation
---

## 2026-04-03 - US-024
- Created audit_logger.rs: background writer thread with mpsc channel for non-blocking logging
- Logs send_text and quick_action messages to ~/.local/share/quip/audit.log (XDG_DATA_HOME aware)
- Log entries include: ISO8601 UTC timestamp, client identifier, message type, text content (first 200 chars)
- Log directory created with 0700 permissions, log file with 0600 permissions
- Log rotation: when file exceeds 10MB, rotates to audit.log.1 (keeps one backup)
- AuditLogger created in start_services(), cloned to GTK handler, passed to handle_incoming_message()
- Files changed: QuipLinux/src/services/audit_logger.rs (new), QuipLinux/src/services/mod.rs, QuipLinux/src/ui/main_window.rs
- **Learnings for future iterations:**
  - `std::sync::mpsc::Sender` is `Clone` — ideal for sharing a logging channel across threads without Arc
  - Avoided adding `chrono` dependency by computing UTC timestamp from SystemTime/UNIX_EPOCH manually
  - XDG_DATA_HOME defaults to ~/.local/share per XDG Base Directory spec — different from XDG_CACHE_HOME (tunnel log) and XDG_CONFIG_HOME (PIN)
  - `std::fs::OpenOptions::new().append(true).open()` is the idiomatic Rust append pattern, matching the tunnel log approach
  - QuipLinux cannot be typechecked on macOS — verified type safety by inspection per established pattern
---

## 2026-04-03 - US-025
- Added QR code URL validation on iOS with warning alert for unrecognized URLs
- `isURLTrusted()` validates: wss://*.trycloudflare.com (tunnel) and ws:// to private IPs (RFC 1918, loopback, link-local, localhost)
- Non-matching URLs trigger an "Unrecognized Server" alert with "Connect Anyway" override and "Cancel" options
- Validation applies to all connection paths (QR scan, manual entry, clipboard paste) since they all go through `doConnect()`
- Files changed: QuipiOS/QuipApp.swift
- **Learnings for future iterations:**
  - SwiftUI `.alert(isPresented:)` with `role: .destructive` on the override button gives the right UX weight for security warnings
  - `URL.host` returns optional and lowercased comparison is needed for case-insensitive domain matching
  - IPv4 private range check via splitting host string on "." and comparing UInt8 octets is simple and avoids importing Network framework
  - The same `doConnect()` path handles QR scan, manual URL entry, and clipboard paste — one validation point covers all entry vectors
  - US-026 (Android equivalent) should use the same patterns: private IP check + trycloudflare.com domain match + warning dialog with override
---
