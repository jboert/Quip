{
  "project": "Quip",
  "branchName": "ralph/qr-pairing-share-link",
  "description": "Make Quip pairing work via a tappable quip://pair share link (AirDrop/Messages/Mail) and obtainable remotely, without depending on the native iOS Camera. Swift/Xcode; quality gate = xcodebuild build + XCTest (no lint/tsc/pytest in this repo).",
  "userStories": [
    {
      "id": "US-001",
      "title": "Unit-test the PairingPayload encode/decode contract",
      "description": "As a developer, I want PairingPayload round-trip + parse coverage so the shared pairing-link format cannot silently regress.",
      "acceptanceCriteria": [
        "Add Shared/Tests/PairingPayloadTests.swift using XCTest with @testable import Quip, wired into the QuipiOSTests target",
        "Test: PairingPayload(url:pin:).encodedURL() then PairingPayload.decode(...) preserves url and pin exactly (round-trip)",
        "Test: a quip://pair?url=<base64-no-pad>&pin=<digits> string decodes to the original ws/wss URL (base64 re-padding works)",
        "Test: malformed input (wrong scheme, missing url, missing or empty pin, non-base64) returns nil",
        "Test: a plain ws:// or wss:// non-quip string decodes to nil so callers fall back to the raw-URL path (matches current contract)",
        "xcodebuild test -project QuipiOS/QuipiOS.xcodeproj -scheme QuipiOS -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:QuipiOSTests/PairingPayloadTests passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Shared/Tests/PairingPayloadTests.swift (13 tests). xcodegen regen wired it into QuipiOSTests; build-for-testing + test-without-building on booted iPhone 17 Pro (id=CFCE8360) both green — 13/13 pass. Headless test runs fine when the sim is already booted (use test-without-building)."
    },
    {
      "id": "US-002",
      "title": "Pair by tapping a quip://pair link (iOS onOpenURL)",
      "description": "As a remote user, I want to tap a shared quip://pair link and have the phone pair automatically, so I never need the on-screen QR or the native Camera.",
      "acceptanceCriteria": [
        "Extract the QRScannerView sheet's decode-and-pair block (QuipiOS/QuipApp.swift around line 1637) into one helper applyPairingPayload(_ payload: PairingPayload) that calls manager.addPaired + KeychainBackendPINs.write + manager.setActive + connect",
        "onOpenURL (QuipiOS/QuipApp.swift around line 310) handles url.host == \"pair\": decode via PairingPayload.decode(url.absoluteString), call applyPairingPayload, and return BEFORE the window/perms branch",
        "The in-app QR scanner sheet calls the SAME helper (no duplicated pairing logic)",
        "Existing quip://window/<id>, quip://perms, and .quippack deep links still work unchanged",
        "xcodebuild build -project QuipiOS/QuipiOS.xcodeproj -scheme QuipiOS -destination 'generic/platform=iOS Simulator' succeeds"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Extracted the QR-sheet decode-and-pair block into applyPairingPayload(_:) on MainiOSView (urlText + manager.addPaired + KeychainBackendPINs.write + manager.setActive + doConnect). QR sheet now calls it. onOpenURL is in the App SCENE struct (separate from MainiOSView, where doConnect/urlText live), so the quip://pair branch decodes then posts the payload via a new .quipApplyPairing Notification.Name (object: PairingPayload); MainiOSView .onReceive runs the SAME helper — mirrors the existing quip://perms → .quipShowSettings bridge. Pair branch returns before perms/window; .quippack/window/perms paths untouched. Gate: xcodebuild build (generic/platform=iOS Simulator) ** BUILD SUCCEEDED **. No new files → no xcodegen/pbxproj change. Native SwiftUI → no browser test; tap-a-shared-link on-device verify = user's pass."
    },
    {
      "id": "US-003",
      "title": "Pairing URL reflects the active network mode",
      "description": "As a remote user, I want the pairing link to embed a URL reachable from where I am, so Tailscale mode uses the ts.net/100.x URL instead of a useless .local address.",
      "acceptanceCriteria": [
        "pairingURL() in QuipMac/Views/SettingsView.swift branches on networkMode: .tailscale -> TailscaleService.webSocketURL (ts.net / 100.x), .cloudflareTunnel -> tunnel.webSocketURL, .localOnly -> LAN/.local",
        "SecurityTab gets TailscaleService and networkMode injected so the branch can read them",
        "In .tailscale mode the embedded URL is the ts.net/100.x address, never ws://<host>.local",
        "Reuse the per-mode URL logic already in ConnectionTab (SettingsView.swift around line 779) rather than inventing a new scheme",
        "xcodebuild build -project QuipMac/QuipMac.xcodeproj -scheme QuipMac -destination 'platform=macOS' succeeds"
      ],
      "priority": 3,
      "passes": true,
      "notes": "pairingURL() in SecurityTab now switch(networkMode): .tailscale -> tailscale.webSocketURL (ts.net/100.x, never .local), .cloudflareTunnel -> tunnel.webSocketURL, .localOnly -> ws://<host>.local:8765 (guard isRunning). Injected TailscaleService via @Environment (already in the SettingsView env chain — ConnectionTab reads the same) + @AppStorage(\"networkMode\") networkModeRaw with a NetworkMode computed, mirroring ConnectionTab. Reuses the per-mode URL sources from ConnectionTab's Diagnostics rows (lanWSURL/tailscaleWSURL/tunnel.webSocketURL) rather than a new scheme. No new files -> no xcodegen/pbxproj change. Gate: xcodebuild build (platform=macOS) ** BUILD SUCCEEDED **. SourceKit 'cannot find X in scope' diags are spurious (no module context); xcodebuild is the oracle. Native SwiftUI -> no browser test; visual QR verify on a Tailscale-mode Mac = user's pass."
    },
    {
      "id": "US-004",
      "title": "Mac 'Send to iPhone' share button",
      "description": "As a user away from my Mac, I want to share the pairing link from the Mac so I can AirDrop/Message/Mail it to the phone and tap to pair.",
      "acceptanceCriteria": [
        "SecurityTab (QuipMac/Views/SettingsView.swift around line 1254) adds a 'Send to iPhone' button",
        "The button builds the quip://pair link via PairingPayload(url: pairingURL(), pin: <current PIN>).encodedURL() plus a human-readable \"<ws url>\\nPIN: <pin>\" text fallback",
        "Tapping it presents NSSharingServicePicker over those items, reusing the existing diagnostics-share pattern (SettingsView.swift around line 1237) rather than a new mechanism",
        "xcodebuild build -project QuipMac/QuipMac.xcodeproj -scheme QuipMac -destination 'platform=macOS' succeeds"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Added a 'Send to iPhone…' AnchoredButton inside SecurityTab's 'Pair iPhone' Section, directly under pairingQRBlock (so US-005 already has it surfaced near the QR). New sharePairingLink() @MainActor helper builds items = [URL(quip://pair?url=<b64>&pin=<digits>) from PairingPayload(url: pairingURL(), pin: pinManager.pin).encodedURL(), \"<ws url>\\nPIN: <pin>\" text fallback] and shows an NSSharingServicePicker pinned to a new @State pairingAnchorView — same pattern as DiagnosticsBundle.presentSharePicker (inlined, not a new mechanism). Button .disabled when pairingURL().isEmpty (server stopped). Reused pairingURL() (US-003 per-mode branch) so the shared link carries the active-mode URL. No new files -> no xcodegen/pbxproj change. Gate: xcodebuild build (platform=macOS) ** BUILD SUCCEEDED **. SourceKit 'cannot find X in scope' diags spurious (no module context); xcodebuild is the oracle. Native SwiftUI -> no browser test; AirDrop/Message/Mail-then-tap on-device verify = user's pass."
    },
    {
      "id": "US-005",
      "title": "Clarify the pairing screen (native Camera cannot open the QR)",
      "description": "As a user, I want the pairing screen to tell me the native Camera cannot scan it, so I use the in-app scanner or the share link instead of getting 'No usable data found'.",
      "acceptanceCriteria": [
        "The QR header copy in SecurityTab no longer implies the native Camera works (e.g. 'Scan with iPhone' becomes 'Scan in the Quip app - the Camera app cannot open it')",
        "The 'Send to iPhone' share button (US-004) is surfaced as the easy/remote alternative near the QR",
        "No functional regression to the QR rendering itself",
        "xcodebuild build -project QuipMac/QuipMac.xcodeproj -scheme QuipMac -destination 'platform=macOS' succeeds"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Copy-only clarification in SecurityTab (QuipMac/Views/SettingsView.swift), no functional change to QR rendering. (1) pairingQRBlock now wraps the QR HStack in a VStack with a leading caption \"Scan in the Quip app — the Camera app can’t open it\" so the warning sits directly above the QR. (2) Section footer rewritten to lead with the explicit Camera warning: \"The iPhone Camera app can’t open this QR (it shows \\\"No usable data found\\\"). Scan it inside the Quip app — qrcode.viewfinder button in the URL bar … Or tap \\\"Send to iPhone\\\" …\". The US-004 'Send to iPhone…' AnchoredButton already sits immediately under pairingQRBlock = the easy/remote alternative surfaced near the QR (AC#2). No new files -> no xcodegen/pbxproj change. Gate: xcodebuild build (platform=macOS) ** BUILD SUCCEEDED **. SourceKit 'cannot find X in scope'/'Generic parameter T' diags are spurious (no module context); xcodebuild is the oracle. Native SwiftUI -> no browser test; visual copy verify on the Mac pairing screen = user's pass."
    }
  ]
}
