# Ralph progress · ralph/swrm-board-integration · started 2026-06-05

PRD: prd.json (source: tasks/prd-swrm-board-integration.md)
Base branch: eb-branch (ralph branch cut from eb-branch, NOT main)

## Codebase Patterns
### iOS PRD (Settings/keyboard/voice UX — eb-branch, current prd.json)
- BUILD GATE (iOS): `cd QuipiOS && xcodebuild build -project QuipiOS.xcodeproj
  -scheme QuipiOS -destination 'generic/platform=iOS Simulator'`. The named QA
  sim UDIDs in memory/progress are ALL STALE (3B2ACF04-… now gone too); the only
  booted-capable sim is iPhone 17 Pro CFCE8360-508E-4A66-B0FD-615D8CD2A549 (OS 26.5).
  For a pure Swift-6 compile gate use `generic/platform=iOS Simulator` — no device
  match needed, no boot. QuipiOS uses a CHECKED-IN .xcodeproj (NOT xcodegen, unlike
  QuipMac) — adding a new Swift file means adding it to the project's build phase
  (Models/Resources/Services/Views are folder refs that glob, so files dropped in
  those dirs auto-join; a brand-new top-level file does not).
- SETTINGS ROWS: the canonical row chrome is `settingsLinkRow(...)` +
  `settingsIcon(_:tint:)` in QuipApp.swift (~:5983/:6008). settingsIcon is the
  28×28 tinted rounded-rect chip (tint.opacity(0.12) bg). There are now TWO
  settingsLinkRow overloads: `trailing: String?` and a generic
  `@ViewBuilder trailing: () -> Trailing`. Swift disambiguates by call site
  (String arg vs trailing closure). Use the closure overload for badge/stateful
  trailings; reuse it instead of hand-rolling an HStack so spacing/chrome stays
  consistent. macPermsSection is INTENTIONALLY not on this chrome (red/green
  semantics) — leave it.
- NATIVE STORIES: per prd.json, the pass gate is xcodebuild succeeding (+ noted
  unit tests). On-device/manual verify is the USER's pass — note it in the story,
  don't block on it. The Ralph "browser testing" step does NOT apply (native SwiftUI).

### Mac/swrm PRD (prior, all 8 passed)
- BUILD: QuipMac uses xcodegen. After adding/removing any source file, run
  `cd QuipMac && xcodegen generate` BEFORE xcodebuild — sources glob `path: .`
  so new files auto-join the target on regen. project.pbxproj is generated (commit it).
- BUILD CMD: `cd QuipMac && xcodebuild build -project QuipMac.xcodeproj -scheme QuipMac -destination 'platform=macOS'`.
- TEST HOST HANGS HEADLESS: `xcodebuild test ...` fails with "test runner hung
  before establishing connection" for ALL suites (the full Quip.app is the
  TEST_HOST and won't launch in this sandbox). NOT a per-test bug. Verify pure
  logic via a standalone `swift file.swift` run; note tests need Xcode/user to run.
- SWIFT 6 (app target SWIFT_VERSION 6.0): a mutable `static var` on a plain
  enum/struct (non-actor) is a concurrency error. Use `nonisolated(unsafe) static var`
  for test-only overrides, OR put the static inside a `@MainActor` type (the
  PromptLibrary.directoryOverrideForTests pattern). Tests run Swift 5 mode.
- App logs: add a `LogPaths.<name>Path` accessor (QuipMac/Services/LogPaths.swift),
  append with the WebSocketServer.wslog idiom (FileHandle seekToEnd / else createFile).
- swrm reader contract lives at ~/Projects/swrm/native/SwrmCore/Sources/SwrmCore/EventLog.swift
  (TS twin: ~/Projects/swrm/src/events/subscribe.ts). Copy, don't reinvent.

## Iteration log

## 2026-06-06 — US-001 (swrm event-log tailer service)
- Added QuipMac/Services/SwrmEventTailer.swift:
  - SwrmEvent / SwrmEventData (data trimmed to {title,from,to} per PRD)
  - SwrmEventReader: read()/readIncremental()/parse — copied verbatim from swrm
    EventLog.swift, torn-trailing-line tolerant (holds back segment after last \n,
    nextOffset never passes last complete-line boundary), seq>since dedupe gate.
  - SwrmCursor + SwrmCursorStore: byte+seq cursor persisted to
    ~/Library/Application Support/Quip/swrm/cursors/<sanitized-root>.json (OUTSIDE .swrm/).
  - SwrmEventTailer (@MainActor @Observable): start()/stop(), fs-watch on the .swrm/
    DIR (not the file — log may not exist / gets atomically replaced) + poll-timer
    fallback (2s). Pumps serialized via isPumping/pumpQueued; file read off-main on
    ioQueue, cursor mutate + onEvents delivery on main. Best-effort, non-throwing.
  - SwrmEventTailer.globalLog → LogPaths.swrmPath (~/Library/Logs/Quip/swrm.log).
- Added LogPaths.swrmPath accessor.
- Added QuipMac/Tests/SwrmEventTailerTests.swift (reader seam + cursor store).
- Files: QuipMac/Services/SwrmEventTailer.swift (new), QuipMac/Tests/SwrmEventTailerTests.swift (new),
  QuipMac/Services/LogPaths.swift, QuipMac/QuipMac.xcodeproj/project.pbxproj (xcodegen regen).
- Gate: xcodebuild QuipMac BUILD SUCCEEDED. Reader/cursor logic ALL PASS via standalone swift run.
- **Learnings for future iterations:**
  - The tailer is delivery-only; title cache (US-003), trigger predicate (US-004),
    push/inject (US-005/007), lifecycle wiring (US-008) consume `onEvents`/`SwrmEventReader.read`.
    SwrmEventReader.read(projectRoot:) is already present for the US-003 cold-start seed.
  - DON'T re-slurp on every tick: readIncremental seeks by byte cursor. Watch the DIR.
  - US-008 first-launch policy: seed cursor to latest seq to avoid a historical-move
    notification storm (set cursor.seq + byteOffset=EOF on a fresh root before tailing).
  - SwrmCursorStore.directoryOverrideForTests + PromptLibrary.directoryOverrideForTests
    are the test-injection pattern for app-support-backed stores.
---

## 2026-06-06 — US-002 (Configurable swrm project roots + Settings UI)
- Added QuipMac/Services/SwrmProjectStore.swift (@MainActor @Observable):
  - roots: [String] persisted to UserDefaults key "swrmProjectRoots" (JSON),
    init load + persist-on-mutation — verbatim PushNotificationService pattern.
  - tailers: [String: SwrmEventTailer] — one per root. add(path:) appends +
    persists + startTailer; remove(path:) removes + persists + stopTailer. LIVE
    (no restart). startAll()/stopAll() present for US-008 but NOT called at init.
  - validate(path:) -> AddError? : .notADirectory if not an existing dir,
    .duplicate if already configured. "can contain .swrm/" ⇒ any existing dir
    qualifies (a dir can always gain a subdir), so existing-dir is the only gate.
  - onTailerEvents hook: var with didSet that rewires every running tailer; the
    seam US-004/US-008 set once to receive (tailer, [SwrmEvent]). wire() installs
    a weak-self/weak-tailer onEvents bridge per tailer.
  - normalize(): expandingTildeInPath + standardizingPath so dupe keys are stable.
- Added SwrmTab to QuipMac/Views/SettingsView.swift + its tabItem ("swrm",
  square.stack.3d.up). Folder-picker add (NSOpenPanel canChooseDirectories),
  inline red error on reject, per-row remove (xmark.circle), ContentUnavailableView
  empty state, footer count. Reads store via @Environment(SwrmProjectStore.self).
- Wired QuipMacApp.swift: @State swrmProjectStore + .environment(swrmProjectStore)
  on the Settings scene.
- Files: QuipMac/Services/SwrmProjectStore.swift (new), QuipMac/Views/SettingsView.swift,
  QuipMac/QuipMacApp.swift, QuipMac/QuipMac.xcodeproj/project.pbxproj (xcodegen regen).
- Gate: xcodebuild QuipMac BUILD SUCCEEDED. No unit test in this story's criteria.
- **Learnings for future iterations:**
  - A new Settings TAB needs THREE edits: (1) a `private struct FooTab: View` in
    SettingsView.swift, (2) its `.tabItem` inside the TabView, (3) if it reads a
    new @Observable store, add `.environment(store)` to the `Settings { SettingsView() }`
    block in QuipMacApp.swift AND a `@State private var store = …` there. Miss (3)
    and the tab crashes at runtime (missing environment), not at compile time.
  - SwrmProjectStore.onTailerEvents is the SINGLE wiring point for US-004 (card),
    US-005 (push), US-007 (inject): set it in a coordinator and it forwards
    (SwrmEventTailer, [SwrmEvent]) for every root. The tailer arg gives you
    projectRoot for terminal-cwd matching (US-006/US-007).
  - US-008 must call store.startAll() in startServicesOnce() and store.stopAll()
    on quit/background — US-002 deliberately does NOT auto-start persisted roots at
    init, only live add/remove. First-launch cursor-seed (US-008) should run BEFORE
    startAll() to avoid the historical-move storm.
---

## 2026-06-06 — US-003 (Story title resolution, file-only)
- Extended QuipMac/Services/SwrmEventTailer.swift (no new files):
  - titleCache: [aggregateID: String] (private(set)). task.moved carries only
    {from,to}, never a title — so titles are learned elsewhere.
  - ingestTitles(from:) absorbs data.title from titleBearingTypes
    {task.created, task.planned}; non-empty title + non-empty id; later wins.
  - resolvedTitle(forAggregateID:) → cached title or "Story #<id>" fallback.
  - seedTitleCache() @discardableResult: one-shot SwrmEventReader.read(projectRoot:)
    full-history slurp at start() BEFORE requestPump. Cursor-INDEPENDENT (reads
    from seq 0) so it recovers titles from a task.created consumed in a prior run
    that now sits behind the byte cursor — the warm-restart case. File-only.
  - Pump now calls ingestTitles(from: result.events) BEFORE onEvents delivery,
    so a same-batch task.moved sees its task.created (events are seq-sorted asc).
- Added 5 @MainActor XCTest cases to QuipMac/Tests/SwrmEventTailerTests.swift
  (created→moved resolves, planned seeds, moved-without-created → Story #<id>,
  unknown id fallback, ingest later-title-wins). writeProjectLog helper writes
  <tmpDir>/.swrm/events.ndjson so a tailer rooted at tmpDir can seed.
- Files: QuipMac/Services/SwrmEventTailer.swift, QuipMac/Tests/SwrmEventTailerTests.swift.
  NO new files ⇒ no xcodegen regen needed (glob only matters when files are added/removed).
- Gate: xcodebuild QuipMac BUILD SUCCEEDED. Cache logic ALL PASS via standalone swift run.
- **Learnings for future iterations:**
  - US-004 trigger handler resolves the card/notice title with
    tailer.resolvedTitle(forAggregateID: ev.aggregateID) — the tailer arg is
    delivered by SwrmProjectStore.onTailerEvents (US-002 seam). No extra plumbing.
  - The title cache is per-tailer (per project root). That's correct: aggregateIDs
    are only unique within a swrm project. Don't hoist it to a global cache.
  - seedTitleCache() is synchronous on @MainActor in start(). Single-project log
    is small (read() doc says cheap), so the brief main-thread blip at start beats
    an async seed racing the first pump. If logs ever get huge, move to ioQueue
    and gate requestPump on seed completion (the ordering invariant: seed before
    any delivery so onEvents handlers can resolve titles).
---

## 2026-06-06 — US-004 (Trigger filter + phone card over WebSocket)
- Shared/MessageProtocol.swift: added SwrmStoryStartedMessage
  {type='swrm_story_started', project, taskId, title, ts} — Codable+Sendable,
  init sets type. Lives in ../Shared so BOTH QuipMac and QuipiOS targets see it.
- QuipMac/Services/SwrmEventTailer.swift: added pure predicate
  `SwrmEvent.isStoryStarted` (type=='task.moved' && data.to=='in_progress').
  Pure ⇒ unit-testable with no I/O; the single trigger US-004/005/007 fan out from.
- QuipMac/Services/SwrmStoryCoordinator.swift (NEW, @MainActor): the single
  fan-out point. handle(tailer:events:) filters batch by isStoryStarted; per
  match resolves title (tailer.resolvedTitle, US-003), project = event.project
  ?? projectRoot.lastPathComponent, broadcasts the card (weak webSocketServer).
  US-005 (push) + US-007 (inject) add methods here; US-008 owns lifecycle.
- QuipMac/QuipMacApp.swift: @State swrmStoryCoordinator; in startServicesOnce
  set coordinator.webSocketServer + swrmProjectStore.onTailerEvents =
  coordinator.handle (the US-002 onTailerEvents seam, finally consumed).
- iOS 3-layer message chain (per multibackend wire() rule):
  (1) WebSocketClient: onSwrmStoryStarted callback + 'swrm_story_started' case
      (isAuthenticated-gated, decodeMessage).
  (2) BackendConnectionManager: onSwrmStoryStarted decl + c.onSwrmStoryStarted
      bridge in wire() (weak self/session).
  (3) QuipApp host: manager.onSwrmStoryStarted → errorToast banner
      'swrm ▶ <title> · <project>' (4s), active-session gated like onError.
- QuipMac/Tests/SwrmEventTailerTests.swift: +4 predicate cases (in_progress
  true; done/todo false; nil-to false; created/completed+in_progress false).
- Files: Shared/MessageProtocol.swift, QuipMac/Services/SwrmEventTailer.swift,
  QuipMac/Services/SwrmStoryCoordinator.swift (new), QuipMac/QuipMacApp.swift,
  QuipMac/Tests/SwrmEventTailerTests.swift,
  QuipiOS/Services/WebSocketClient.swift, QuipiOS/Services/BackendConnectionManager.swift,
  QuipiOS/QuipApp.swift, both project.pbxproj (xcodegen regen — Mac had a new file).
- Gate: xcodebuild QuipMac BUILD SUCCEEDED + xcodebuild QuipiOS BUILD SUCCEEDED.
- **Learnings for future iterations:**
  - QA SIM UDID CHANGED: the dedicated Quip QA sim is now
    3B2ACF04-1B0A-4842-827C-5B1699B8D4F8 (OS 26.4.1) — the memory's
    D853A014-… is stale/gone. Use `id=3B2ACF04-…` for QuipiOS sim builds.
  - SwrmStoryCoordinator is THE extension point for US-005/US-007: add
    sendPush(...) / injectNotice(...) calls inside handleStoryStarted, each
    independently best-effort (wrap so one failure doesn't suppress the card).
    It already has tailer (→ projectRoot for US-007 cwd match) + resolved title.
  - A new Mac→iOS message needs FOUR touch points: (1) the struct in
    Shared/MessageProtocol.swift, (2) WebSocketClient case+callback,
    (3) BackendConnectionManager decl+wire() bridge, (4) QuipApp host hook.
    Miss (3) and the host hook silently never fires (multibackend wire rule).
  - US-004 reuses errorToast as the card surface (zero new render plumbing).
    If a dedicated styled swrm card is wanted later, add a @State swrmCard in
    QuipApp + a binding through MainiOSView like errorToast at line ~204.
  - onTailerEvents fires only for RUNNING tailers; US-002 starts them live on
    add. So US-004 cards work for a root added this session; persisted-root
    start-at-launch (so cards fire without a re-add) is US-008's startAll().
---

## 2026-06-06 — US-005 (APNs push for a started story)
- PushNotificationService.swift (no new file):
  - buildSwrmPayload(project:taskId:title:sound:) — pure static builder
    paralleling buildPayload. aps.alert.title "Started - <project>",
    aps.alert.body "<title> -> In Progress", aps.sound "default" iff sound.
    Top-level quip_event="swrm_story_started" + quip_swrm_task_id=<id>. NO
    badge/category (a story-started alert isn't an attention queue).
  - notifySwrmStoryStarted(project:taskId:title:) — device loop. Reuses the
    SAME per-device gating subset as notifyWaitingForInput: paused,
    bannerEnabled, isQuietNow. DELIBERATELY drops the selection/all-windows
    gate (swrm events aren't tied to a phone-selected terminal) and the 30s
    debounce (story moves are discrete user actions; collapseId coalesces
    repeats). collapseId="swrm-<project>-<taskId>". APNsError.unregistered ->
    removeDevice. Payload encoded to Data on main, then per-device fire-and-
    forget Task (Swift-6 Sendable boundary, same trick as the waiting path).
- SwrmStoryCoordinator.swift: + weak pushNotificationService; handleStoryStarted
  fires the push AFTER the US-004 broadcast. Each side-effect independent +
  best-effort (a push failure never suppresses the card).
- QuipMacApp.swift startServicesOnce: swrmStoryCoordinator.pushNotificationService
  = pushNotificationService (one line next to the webSocketServer wire).
- Tests/PushPayloadShapeTests.swift: +2 cases (swrm payload title/body/extras
  + no badge/category; sound:false omits sound).
- Files: PushNotificationService.swift, SwrmStoryCoordinator.swift,
  QuipMacApp.swift, Tests/PushPayloadShapeTests.swift. NO new files ⇒ no
  xcodegen regen.
- Gate: xcodebuild QuipMac BUILD SUCCEEDED. Payload verified via standalone swift.
- **Learnings for future iterations:**
  - There are now TWO push entry points on PushNotificationService:
    notifyWaitingForInput (terminal state) and notifySwrmStoryStarted (swrm).
    They SHARE cachedClient + the prefs gating helpers but have different
    gate subsets — don't fold them together; the waiting path's selection +
    debounce gates are wrong for discrete swrm events.
  - US-005 is Mac-only. The payload carries quip_event + quip_swrm_task_id so
    a FUTURE iOS story could deep-link a tapped swrm push, but US-005 itself
    requires no QuipiOS change (the in-app card is US-004's WebSocket path).
  - SwrmStoryCoordinator.handleStoryStarted is now the 2-of-3 fan-out (card +
    push). US-007 adds injectNotice(...) here as the third best-effort limb;
    it has `tailer.projectRoot` for the cwd match (US-006 captures cwdPath).
  - To add another push class, copy the buildSwrmPayload + notify* pair shape:
    one pure builder (unit-test in PushPayloadShapeTests) + one device loop
    that picks only the gates that make sense for that event class.
---

## 2026-06-06 — US-006 (Capture full terminal working-directory path)
- QuipMac/Services/WindowManager.swift (no new files):
  - ManagedWindow: + `var cwdPath: String?` — full iTerm cwd, nil for
    Terminal.app / unknown. subtitle stays the basename (display unchanged).
  - New nested `struct SubtitleInfo: Sendable {subtitle, cwdPath}`. fetchSubtitles()
    return type String -> SubtitleInfo. The AppleScript already read iTerm's
    `path` var in full; the old code threw it away via lastPathComponent. Now
    subtitle = basename, cwdPath = path (nil when empty).
  - applySubtitles(): sets both subtitle + cwdPath from SubtitleInfo. Terminal.app
    branch unchanged (basename-only subtitle; cwdPath stays nil — no shell-var access).
  - Both ManagedWindow init sites carry cwdPath (existing.cwdPath / nil for new).
  - QuipMacApp snapshot loop (line ~472/481) passes the dict through by type
    inference — no annotation, so the return-type change is transparent there.
- Files: QuipMac/Services/WindowManager.swift only. NO new files -> no xcodegen regen.
- Gate: xcodebuild QuipMac BUILD SUCCEEDED.
- **Learnings for future iterations:**
  - US-007 cwd match: find tracked window with `cwdPath == projectRoot` (exact;
    Terminal.app windows have cwdPath==nil so they auto-skip; if multiple match,
    most-recently-active). Empty iTerm path was mapped to nil (NOT "") on purpose
    so a blank cwd never collides with a real root.
  - SwrmStoryCoordinator already has `tailer.projectRoot` (US-002 seam) — that's
    the value to compare against `window.cwdPath`. Inject via KeystrokeInjector
    using the window's iterm2SessionId (US-007).
  - ManagedWindow is `@unchecked Sendable`; optional `var` stored props default
    to nil in the synthesized memberwise init, so QAModeBroadcastTests's mw()
    helper compiled without adding cwdPath. Don't bother passing it in tests
    unless a test needs a specific cwd.
  - cwdPath only refreshes on the subtitle cycle (every ~5th snapshot tick,
    QuipMacApp needSubtitles gate) — same cadence as subtitle. A just-opened
    iTerm window can have stale/nil cwdPath for up to one subtitle cycle.
---

## 2026-06-06 — US-007 (Inject a notice line into the matching terminal)
- SwrmStoryCoordinator: added weak `windowManager` + weak `keystrokeInjector`
  (owned by the app scene; nil => inject side-effect no-ops). handleStoryStarted
  now calls injectNotice(rootPath:taskId:title:) AFTER the US-004 card + US-005
  push — each side-effect independent + best-effort.
- injectNotice:
  - target = Self.matchTerminal(windows: windowManager.windows, rootPath:
    tailer.projectRoot.path, zOrderIds: WindowManager.fetchWindowList().map(\.id)).
  - none -> globalLog "no tracked terminal" + skip.
  - matched but iterm2SessionId == nil -> globalLog skip (MVP iTerm2-only; no
    focus-stealing front-window fallback). Terminal.app cwdPath is always nil so
    it can't match here anyway — this branch really covers an unresolved iTerm
    session.
  - else keystrokeInjector.sendText(notice, pressReturn:false, terminalApp:.iterm2,
    iterm2SessionId:) — the iTerm2 session-id AppleScript path writes via
    `unique id` WITHOUT `activate`, so it never steals focus; pressReturn:false
    => the line is never submitted.
- noticeLine(title:taskId:) = '# swrm: started "<title>" (#<id>)' — leading # so a
  shell/REPL treats it as a comment if the user later hits return.
- matchTerminal(windows:rootPath:zOrderIds:) + canonicalPath are static & pure:
  filter by cwdPath canonical-equality to rootPath; on >1 match pick the one
  earliest in zOrderIds (CG front-to-back == most-recently-active; off-screen ids
  absent from zOrder sort last via Int.max).
- Wired swrmStoryCoordinator.windowManager + .keystrokeInjector in
  QuipMacApp.startServicesOnce (alongside the existing webSocketServer + push).
- Files: QuipMac/Services/SwrmStoryCoordinator.swift, QuipMac/QuipMacApp.swift.
  NO new files -> no xcodegen regen, no pbxproj churn.
- Gate: xcodebuild QuipMac BUILD SUCCEEDED. Logic verified via standalone swift
  run (9/9: exact, trailing-slash, no-match, nil/empty cwd skip, z-order
  frontmost, off-screen-sorts-last, notice format, fallback title).
- **Learnings for future iterations:**
  - CGWindowListCopyWindowInfo(.optionOnScreenOnly) returns front-to-back z-order,
    which IS macOS recency order — use it as the "most-recently-active" tiebreak
    instead of adding new per-window timestamp tracking. But windowManager.windows
    is reordered by the user's customOrder (sidebar), NOT z-order, so you must
    re-fetch fetchWindowList() at decision time to get recency.
  - iTerm2 sendText with iterm2SessionId set never activates iTerm (writes by
    `unique id`) -> the only focus-safe inject path. Terminal.app path DOES
    activate; never route swrm notices there.
  - cwdPath comparison must be canonicalized both sides (tilde/.././trailing-slash)
    — iTerm's `path` var and the normalized store root can differ by a trailing /.
  - US-008 remaining: start tailers at launch + stop on quit/background
    (swrmProjectStore.startAll()/stopAll() already exist, NOT yet called), and the
    first-launch cursor-to-EOF seed to avoid a historical-move storm.
---

## 2026-06-06 — US-008 (Lifecycle wiring + end-to-end verification)
- QuipMacApp.startServicesOnce: after wiring the coordinator (US-004/005/007),
  call swrmProjectStore.startAll() so a tailer starts for EVERY persisted root
  at launch (US-002 only started them live on add — so cards/pushes fired only
  for a root added that session). Added a NotificationCenter.default observer on
  NSApplication.willTerminateNotification -> swrmProjectStore.stopAll() to release
  fs-watch fds + poll timers on quit. NOTE: willTerminate posts on
  NotificationCenter.default, NOT NSWorkspace.shared.notificationCenter (the
  workspace center used for didActivateApplication/didWake in the same method).
- SwrmEventTailer first-launch storm guard:
  - SwrmCursorStore.exists(forRootPath:) — does a cursor file already exist?
  - SwrmEventTailer captures `hadPersistedCursor` in init.
  - start() calls new seedCursorToLatest() ONLY when !hadPersistedCursor. It runs
    readIncremental(byteOffset:0, since:0), sets cursor.byteOffset = nextOffset
    (last COMPLETE-line boundary; torn trailing line held back) + cursor.seq =
    max seq, persists — WITHOUT delivering. So configuring a project mid-history
    doesn't replay every past task.moved->in_progress as a card/push/inject storm.
  - seedTitleCache() still runs first and is cursor-INDEPENDENT (reads seq 0), so
    a later real move still resolves its title even though history wasn't delivered.
- Files: QuipMac/Services/SwrmEventTailer.swift, QuipMac/QuipMacApp.swift.
  NO new files -> no xcodegen regen.
- Gate: xcodebuild QuipMac BUILD SUCCEEDED + xcodebuild QuipiOS BUILD SUCCEEDED.
  No new warnings in changed files. Seed/no-storm logic verified via standalone
  swift run (seed skips all 4 history lines incl. an in_progress move ×3, torn
  tail held, post-seed pump delivers ONLY genuinely-new seqs [5,6], in_progress
  storm = [5] only — ALL PASS).
- **Learnings for future iterations:**
  - Two cursor "zero" states are NOT equivalent: a missing cursor FILE (fresh
    root -> seed to EOF, skip history) vs a persisted {0,0} (resume from start).
    SwrmCursorStore.load() returns .zero for both; SwrmCursorStore.exists() is the
    discriminator. Don't collapse them or you reintroduce the first-launch storm.
  - The first-launch seed reuses readIncremental's torn-line discipline instead
    of stat()'ing file size — so a half-written trailing line at configure time
    isn't swallowed; it's held back and delivered whole on the next real pump.
  - macOS app teardown: NSApplication.willTerminateNotification is the reliable
    quit hook for a menubar app (there's no meaningful "background"); it fires on
    NotificationCenter.default. The closure is @Sendable under Swift 6 — use
    MainActor.assumeIsolated to touch the @MainActor store (we scheduled it on
    queue:.main so the assume is sound), same idiom as the 2s snapshot timer.
  - ALL 8 user stories now pass. The integration is start-to-finish: persisted
    roots tail at launch -> story moved to in_progress -> coordinator fans out
    phone card (US-004) + APNs push (US-005) + iTerm notice (US-007), each
    best-effort; quit stops every tailer cleanly.
---

## 2026-06-16 — US-001 (Settings row visual consistency) [iOS PRD]
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- New PRD on eb-branch (Settings/keyboard/voice UX, iOS-only). progress.txt above
  is from the prior swrm PRD — kept for pattern history.
- QuipiOS/QuipApp.swift:
  - Added a generic `settingsLinkRow<Trailing: View>(title:subtitle:systemImage:
    tint:@ViewBuilder trailing:)` overload right after the existing `trailing:
    String?` one. Identical icon-chip chrome (settingsIcon + title/subtitle VStack
    + Spacer), but renders an arbitrary trailing view. Overload resolution: a
    trailing closure picks this; `trailing:` String picks the old one — existing
    Notifications/Connection callers untouched.
  - Latency row (was a hand-built HStack spacing:8) → settingsLinkRow with
    `LatencySummary.from(samples:).badgeView()` as the trailing closure.
  - Version button → settingsLinkRow(info.circle, .gray) with the if versionCopied
    "Copied".transition(.opacity) else appVersionDisplay.monospacedDigit() as the
    trailing closure. Kept .buttonStyle(.plain) + tap-to-copy action + accessibility
    hint, so the copy + Copied confirmation transition behave identically.
  - macPermsSection left unchanged (red/green semantics intentional).
- Files changed: QuipiOS/QuipApp.swift, prd.json, progress.txt.
- Gate: `xcodebuild build -project QuipiOS.xcodeproj -scheme QuipiOS -destination
  'generic/platform=iOS Simulator'` → ** BUILD SUCCEEDED **. No new warnings in
  changed lines (only pre-existing deprecation warnings elsewhere in the file).
  Manual on-device verify (Settings → Diagnostics chip parity, badge intact,
  Version still copies) is the user's pass per PRD.
- **Learnings for future iterations:**
  - US-002 (CustomButtonForm live pill preview) and US-003 (hold-any-slash palette)
    are the next two. CustomButtonForm is at ~:7063, the customQuickButton pill
    chrome to reuse is at ~:4412, slotRowView at ~:4241, SlashGroupMember at ~:4085
    (line numbers will drift after this edit — re-grep, don't trust them blindly).
  - QuipiOS is a checked-in .xcodeproj, not xcodegen. No `xcodegen generate` step.
    New top-level Swift file (e.g. US-005's TranscriptCorrector.swift / its test)
    must be added to the QuipiOS (and QuipiOSTests) build phase manually — verify
    via xcodebuild after, since a missing membership compiles green but ships nothing.
  - For UI stories, prefer reusing settingsLinkRow / settingsIcon (and the pill
    chrome at :4412) over new HStacks — the PRD's whole US-001 theme is "one chrome".
  - QA sim is gone again; `generic/platform=iOS Simulator` is the reliable
    boot-free compile gate. Don't chase the memory's UDIDs.
---

## 2026-06-16 — US-002 (Custom-button add-key clarity) [iOS PRD]
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- QuipiOS/QuipApp.swift, struct CustomButtonForm:
  - Added a top "Preview" Form section with `previewPill` — a standalone view
    that MIRRORS the customQuickButton row chrome (9pt monospaced label OR
    16x16 SF Symbol, .foregroundStyle(colors.chipText), pad 4x5, minWidth 20 /
    minHeight 28, chipFill bg, RoundedRectangle radius 5). It is a visual copy,
    NOT a call into the host's customQuickButton(_:) (that method needs the
    host's `colors`/`selectedWindowId`/fireCustomButton env; the form is a
    separate struct). Added `@Environment(\.colorScheme)` + a computed
    `colors: QuipColors` to get the same chip palette as QuickButtonsSheet.
  - Live label auto-fill: new `labelEdited` @State gate. The Label TextField now
    binds through a custom Binding whose setter flips labelEdited (true on a
    non-empty manual edit, false when cleared). `autofillLabelIfNeeded()` writes
    `label` DIRECTLY (bypassing that setter, so it isn't seen as a user edit)
    and is fired from `.onChange(of: text/keystroke/payloadKind)`. derivedLabel()
    = slash->command word after '/', text->first 8 chars, keystroke->option
    label. hydrate() sets labelEdited=true for an existing button so editing
    never auto-clobbers the saved label.
  - Action Section converted to header/footer form; `actionFooter` is a
    @ViewBuilder switch on payloadKind (Slash footer carries the auto-submit
    guidance + the "/x… auto-group" tip; Text + Keystroke have their own).
  - SF Symbol field placeholder -> "SF Symbol e.g. bolt.fill (optional)" + a
    Label-section footer ("blank -> auto-fill"; "symbol replaces label").
  - QuickButtonsSheet: empty-state copy -> "Tap + → “Custom Button” to make your
    own, or pick a built-in." + a footer on the add sheet's first section
    pointing at Custom Button.
- No wire-format change: CustomButton / CustomPayload / customButtonsJSON
  untouched. save()/isValid logic unchanged (auto-fill just guarantees a
  non-empty label sooner, which Save already required).
- Files: QuipiOS/QuipApp.swift, prd.json, progress.txt.
- Gate: `xcodebuild build -project QuipiOS.xcodeproj -scheme QuipiOS
  -destination 'generic/platform=iOS Simulator'` -> ** BUILD SUCCEEDED **.
  Manual on-device verify (live preview/label, footers, Save→fires) = user's pass.
- **Learnings for future iterations:**
  - The "auto-fill that yields to a manual edit" pattern: keep the @State the
    TextField shows, but write user edits through a Binding setter that records
    an `edited` flag, and write programmatic fills to the @State DIRECTLY. The
    setter never sees the programmatic write, so the flag stays clean and the
    fill keeps tracking until the user actually types. Clearing the field flips
    the flag back off (resume auto-fill).
  - QuipColors(scheme: colorScheme) is the shared chip palette — any view that
    wants the keyboard-row pill look needs `@Environment(\.colorScheme)` + that
    computed `colors`, then chipFill/chipText. Defined in Views/QuipTheme.swift.
  - The pill chrome is duplicated in THREE spots now: host customQuickButton(_:)
    (:4412), QuickButtonsSheet.customPillPreview/builtinPillPreview, and now
    CustomButtonForm.previewPill. They're intentionally separate (different
    enclosing structs / env) but MUST stay visually in sync — font 9 mono /
    pad 4x5 / radius 5 / minWidth 20. If you restyle the row pill, update all three.
  - US-003 (hold-any-slash palette) is next: slotRowView ~:4241, SlashGroupMember
    ~:4085 — re-grep, line numbers drifted again after this edit.
---
