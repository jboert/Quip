# Session handoff — 2026-05-05

Auto-generated by the recap-at-50pct hook (~716K tokens of 1M Opus 4.7 window). Supersedes the earlier 2026-05-05 handoff at `98ca0b8`. The earlier session shipped §B13 + §22 + §25 + §23 + §27 + §15 v1+v2 path A; this continuation focused on tail/cleanup tasks ("scrub the wishlist, fold tail items into already-deployed code, fix fresh-install QA on the simulator").

**Resume one-liner sits at the very bottom — paste into a new session after `/clear`.**

---

## Today's commits (this continuation, since `98ca0b8`)

| Hash | Branch tip | Why |
|------|-----------|-----|
| `2dfccc8` | Mac+tests | §49 follow-up — `LogRedactor` masks IPv4 dotted quads (LAN/Tailscale CGNAT/public, last two octets) before each log file lands in the diagnostics bundle. `systemInfoText()` now ships an 8-char salted SHA256 of the host name (`<redacted> (id=…)`) instead of the raw machine name. Retires the stale `// TODO: redact tunnel URLs / device tokens before share` marker — push tokens were already `prefix(8)`-trimmed at every `quipPushLog` call site. **§22 follow-up bundled in same commit:** `MacPermissionsStoreTests` covers the menubar warning glyph aggregation logic — 8 cases (nil / all-granted / all-denied / single / pair / replace) so adding a fourth TCC perm later can't silently regress the rollup. |
| `8fdbd66` | docs | Wishlist stamp `2dfccc8` + records §27 dedupe-wiring audit result (no-op): grepped every side-effecting Mac-side handler post-§27 ship; all 7 (`send_text` / `quick_action` (covers `test_push`) / `duplicate_window` / `close_window` / `spawn_window` / `paste_prompt` / `attach_iterm_window`) still wrap `messageDedupe.checkAndRecord`. No new side-effecting message types added between `e26a8f6` and `b46b45d`. |
| `25d0c03` | docs | Wishlist scrub — 1520 → 705 lines (54% reduction). Collapsed every ✅ Done entry to status line + commit hash + 1-2 sentence summary. Dropped "Original context (kept for historical reference)" / per-section "Acceptance test" / "Files" / "Related code" / verbose v2 deferred-feature lists from done items (git log + the code itself are the source of truth). Wishlist / In Progress / Blocked items kept full. Tabled §55 Clipboard sync (Apple Universal Clipboard / Handoff already covers Mac↔iPhone on shared iCloud + BT + Wi-Fi). 70 section headings preserved. No commit hashes lost. |
| `43dbbc5` | iOS+tests | §B14 (new) — fresh-install QA seed. New `QuickSlotStore.defaultSlots(demoCustomID:)` returns starter row `/btw, [demo custom], spacer, Y, N, spacer, 1, 2, spacer, Esc, Backspace`. New `CustomButtonStore.defaultDemo()` returns single seeded custom button with stable UUID `DEAD1DEA-…-0001`, label `/help`, SF Symbol `wand.and.stars`, slash payload `/help ` (autoSubmit off). Wired into `MainiOSView.onAppear`: when `quickSlotsJSON` empty AND `customButtonsJSON == "[]"` AND `enabledQuickButtonsRaw` is its default → seed both. Otherwise fall through to existing CSV migration so upgraders are untouched. 7 new tests in `QuickSlotStoreSeedTests`. iOS CFBundleShortVersionString 1.4.0 → 1.5.2. |
| `50cd6dc` | docs | Wishlist stamp `43dbbc5` for §B14. |
| `a624a87` | docs | Redact "Tim apple 17" → `<your-iphone>` in README + 2 plan docs + handoff. New `feedback_redact_device_names.md` memory rule prevents future leaks. |
| `2264f08` | docs | Refresh stale commit hashes in handoff + wishlist after `git filter-repo` rewrote `71af40e`'s message body to also redact the device name (8 hash mappings updated). Force-pushed eb-branch with `--force-with-lease`. |
| `28b15e4` | gitignore | Broaden `.claude/` exclusion (was just `settings.local.json`). User memory + transcripts already live outside repo at `~/.claude/projects/...` but defense-in-depth: any `.claude/` at repo root stays local-only. |
| `9ebd685` | iOS | Up-arrow ↔ Return ⏎ parity — both call same submit path when text/image present; Return falls through to bare press_return when input empty. New `canSubmitInput` + `submitOrPressReturn()` helpers. |
| `bcaa529` | iOS | Wrap portrait slot row in horizontal `ScrollView`. Adding chips beyond screen width scrolls instead of pushing parent VStack wider and clipping content. |
| `4d9dd3a` | iOS | Real cause of "blown out" page on iPhone 17 Pro Max: main-row buttons summed to ~460pt vs 430pt screen width. Shrunk btnW 56→48 / pttW 72→64 / navW 26→24. New mainRow total 400pt + Spacers fits. |
| `4eaab35` | iOS | Drop `systemImage` from §B14 demo so the `/help` label renders as visible text instead of unmarked ✨ pill (`customQuickButton` renders icon OR label, never both). |
| `031ddc2` | iOS | Light-mode chip colors + Settings → Appearance Auto/Light/Dark picker. New `colors.chipFill` + `colors.chipText` tokens on `QuipColors` (Dark: white.opacity(0.15); Light: black.opacity(0.10)). 5 chip render sites swapped from hardcoded `Color.white.opacity(0.15)` to tokens. New `@AppStorage("appearance.mode")` applied via `.preferredColorScheme()` on root WindowGroup. |
| `9ef47de` | docs | File §B15 wishlist — iPhone a11y hygiene findings from QA pass (1 critical missing image alt, 31 warnings missing button traits). Low priority sweep PR. |
| `c6eb974` | qa | Add `docs/qa-baselines/main-{light,dark}.png` + README. Captured via ios-simulator-skill. Use `visual_diff.py` against these post-release to catch theme regressions like the chip-fill bug just fixed. |

11 additional commits since the prior handoff line. **All pushed** to `origin/eb-branch` (force-pushed once after `filter-repo` rewrite of historical commit `71af40e` to redact the device-name leak — see `2264f08`).

13 total commits in the after-handoff continuation (`2dfccc8` → `c6eb974`). 16 commits ahead of `main`.

## Why each commit folds into already-deployed work (the tail story)

User asked: "are any of these tail or can any of these items be cleaned up with things we've already deployed?" Three candidates emerged from grepping recently-shipped surfaces:

1. **§49 redact** — DiagnosticsBundle.swift had a year-old TODO ("redact tunnel URLs / device tokens before share"). User clarified they're not running cloudflared currently (Tailscale + LAN only), so the literal "tunnel URL" framing was stale, but the bundle still leaked Tailscale `100.x.x.x` IPs (via `connection.endpoint` writes in WebSocketServer.swift) and the host's machine name (via `Host.current().localizedName`). Fix shipped as `2dfccc8`.

2. **§22 follow-up tests** — `MacPermissionsStore` (shipped `40af168`) had no unit tests on the aggregation logic that flips the menubar from `waveform.circle.fill` to `exclamationmark.triangle.fill`. Pure function, easy to lock under tests. Bundled into `2dfccc8`.

3. **§27 dedupe wiring audit** — Verified that every side-effecting Mac handler post-`e26a8f6` still wraps `messageDedupe.checkAndRecord`. Result: clean. Doc-only stamp at `8fdbd66`.

Then the user asked for a "scrub" of the wishlist to remove redundancies (`25d0c03`).

Then a 4th item emerged after `xcrun simctl io screenshot` revealed the user's "where are my custom buttons?" symptom: fresh-install simulators show no custom-button pill because (defs ↔ slots) is a two-table model and brand-new users have neither table populated. `43dbbc5` seeds both on truly-fresh install so a `simctl erase`'d sim sees the feature out-of-box without a Settings drill-down. The stable `DEAD1DEA-…-0001` UUID prevents seed-path re-entry from duping.

## Test counts after today's continuation

| Suite | Before this continuation | After | Delta |
|-------|--------------------------|-------|-------|
| QuipMacTests | ~233 | 249 | +16 (11 LogRedactor + 8 MacPermissionsStore + 2 systemInfoText additions; one duplicate gets covered by both LogRedactor and DiagnosticsBundle systemInfoText assertions) |
| QuipiOSTests | 178 | 185 | +7 (QuickSlotStoreSeedTests) |
| **Combined** | **411** | **434** | **+23** |

All green, both schemes, end of session. Run times: Mac 13.6s, iOS 7.3-7.4s.

## Install state

| Surface | Version | Last touched |
|---------|---------|--------------|
| `/Applications/Quip.app` | 1.5.1 | NOT rebuilt this continuation (still on prior session's `b46b45d` tip; no Mac-app code changed since — only `LogRedactor.swift` + `DiagnosticsBundle.swift` + tests, which build into the SAME .app version 1.5.1) |
| iOS <your-iphone> (iPhone 17 Pro Max physical) | Debug build of `5e1f487` (1.4.0) | NOT installed this continuation — physical device still on prior tip |
| iPhone 17 Pro Max simulator (`999A4B3C-…`) | Debug build of `43dbbc5` (1.5.2) | This continuation; fresh-install seed verified via plist (NOT visually — Speech permission dialog blocks the screenshot path; AppleScript can't reach iOS-sim UI contents) |
| iPhone 17 simulator (`8CDE7F01-…`, erased) | Debug build of `43dbbc5` (1.5.2) | This continuation; `simctl erase`'d to wipe inherited prefs, then plist-verified seed populates `customButtonsJSON` with `/help` def + `quickSlotsJSON` with 11-slot row including the `.custom(DEAD1DEA-…)` entry |
| iPhone 17 Pro simulator (`C5E371CA-…`) | Debug build of `43dbbc5` (1.5.2) | This continuation; auto-installed during test runs |
| Apple Watch Ultra 3 | Mirrors iOS notifications via system forwarding | n/a |

**Important:** physical iPhone is STILL on `5e1f487` (1.4.0). To see the §B14 seed on hardware, install the latest build. To see `/help` pill on existing physical device, the user can manually create a custom button via Settings → Quick Buttons → + → Custom Button — the seed only runs for truly-fresh installs (their existing `quickSlotsJSON` is already populated, so the seed gate doesn't trip).

## Hardware-verified ✅ vs install-only ⚠️ (carry-forward + new)

**Verified live this session (autonomous, no user hardware):**
- §49 redaction — 11 LogRedactor unit tests + 2 systemInfoText assertions cover Tailscale 100.x.x.x / LAN / public IPv4 / hostname-hash paths ✅
- §22 aggregation — 8 MacPermissionsStore unit tests cover all denial permutations ✅
- §27 audit — grep confirms wiring intact post-`b46b45d` ✅
- §B14 seed — plist verified on `simctl erase`'d iPhone 17 sim ✅

**Install-only — needs your eyes:**
- §B14 visual — pair the iPhone 17 (sim, currently booted with /help seeded) to the local Mac → wait for `auth_result success` + `layout_update` → confirm the slot row renders `/btw  /help  Y N  1 2  Esc ⌫` with `/help` shown as a purple pill with wand-and-stars icon. Tap = paste `/help ` into selected window without submit. Edit/delete via Settings → Quick Buttons → tap pill in editor.
- §22 menubar — revoke Accessibility for Quip in System Settings on the Mac → menubar icon flips to ⚠️ red triangle within 5s; "Permissions needed" section appears with click-to-open button. (Same caveat as the prior handoff — TCC re-grant is painful, hold this for a maintenance moment.)
- §15 v2 — "1" / "2" buttons confirmed visually but not yet seen in audit.log; "All Windows" toggle behavior; Watch tap (only iPhone-side `press_y` confirmed in earlier session).
- Carryovers from 2026-05-04: §53 v1 Watch glance, §50 v1 QR pairing, §57 v2 prompt CRUD, §B3 keyboard pills.

## Open threads (no in-flight code)

Working tree is clean at `50cd6dc`. Decisions still open in backlog:

- **§0c** — PTT recognizer Settings picker + Whisper model-size selector. Could leverage the §15 v2 sectioned-Settings pattern shipped in the prior session.
- **§51** — iCloud KVS sync of paired backends. Needs second device.
- **§54** — Wake-word PTT. Open stack decision: SFSpeech / Picovoice / Whisper-tiny streaming.
- **§56** — Voice macros ("ship it" → multi-step). Open UX shape.
- **§53 v2** — Full Watch slash-button send-back via WCSession reverse channel. The §15 v2 path A landed this AM removed the urgent need.

§55 Clipboard sync is now **tabled** — Apple Universal Clipboard already covers it.

## Known caveats / gotchas to remember

(Inherited from prior handoff — still applicable.)

- **iOS UNNotificationCategory registration is per-cold-launch.** After installing a new build with category changes, force-quit + relaunch is required for action buttons to appear on alerts.
- **`NSWorkspace.shared.notificationCenter` is NOT `NotificationCenter.default`** — Workspace notifications post through their own bus.
- **`xcodebuild -scheme QuipMac` runs from `QuipMac/` cwd**; the iOS scheme equivalent uses `QuipiOS/`. Mismatching cwds produces "directory does not contain an Xcode project" — but builds OK from repo root with `-project` flag explicit.
- **iOS sim preserves UserDefaults across `simctl uninstall`** — only `simctl erase` actually wipes prefs. Important for fresh-install QA.
- **AppleScript / AX cannot reach iOS-sim UI contents** — sim is one AXImage to the host. Permission dialogs etc. need actual user tap; no programmatic dismissal from CLI.
- **`xcrun simctl privacy` does NOT support `speech-recognition`** — `microphone` works but Speech is not in the supported services list. Permission dialog must be tapped manually.
- **Mac stable-signing recipe is non-negotiable** — never `rm -rf /Applications/Quip.app`. Use the 4-step sequence in `reference_quip_install_recipe.md`.
- **`feedback_eb_branch_push_policy.md` overrides hook prompts** — never push to GitHub without explicit user confirmation, even when an automated hook says "Push." in its message.

## Resume one-liner

> Continue Quip work on `eb-branch` from `c6eb974`. The 2026-05-05 continuation shipped 13 commits: §49 redaction + §22 perms-store tests + §27 dedupe audit + wishlist scrub (1520→705 lines) + §B14 iOS fresh-install seed + device-name redact across docs (filter-repo'd `71af40e` and force-pushed) + `.claude/` gitignore broadening + up-arrow ↔ Return ⏎ parity + slot row horizontal ScrollView + main row 430pt fit + `/help` demo label-only render + light-mode chip tokens + Settings → Appearance Auto/Light/Dark picker + §B15 a11y wishlist + visual-diff baselines under `docs/qa-baselines/`. **All pushed** to `origin/eb-branch`. 435 tests green (249 Mac + 186 iOS). 16 commits ahead of `main` — open a PR to merge when ready. Physical iPhone 17 Pro Max running `1.5.2` (installed 14:55 PT). /Applications/Quip.app still 1.5.1 (no Mac-app version bump this continuation — Mac LogRedactor + DiagnosticsBundle changes build into the same 1.5.1 .app). Quip QA simulator: UDID `D853A014-E5D8-46F1-A81D-37860AA9DFA2` ("Quip QA — iPhone 17 Pro Max 26.4") — dedicated cloned device per `feedback_default_qa_simulator.md`.
